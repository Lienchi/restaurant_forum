<% if user != current_user %>
  <% if current_user.is_following?(user) %>
    <%= link_to 'Unfriend', followship_path(user), method: :delete, class: "btn btn-primary" %>
  <% else %>
    <%= link_to 'Friend', followships_path(following_id: user.id), method: :post, class: "btn btn-default" %>
  <% end %>
<% end %>